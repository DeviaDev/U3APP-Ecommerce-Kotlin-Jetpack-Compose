<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.U3App"
        tools:targetApi="31">

        <!-- SplashActivity sebagai launcher -->
        <activity
            android:name="com.devianest.u3app.activity.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.U3App.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AuthActivity untuk Login dan Register -->
        <activity
            android:name="com.devianest.u3app.AuthActivity"
            android:exported="false"
            android:theme="@style/Theme.U3App.NoActionBar" />

        <!-- MainActivity untuk aplikasi utama -->
        <activity
            android:name="com.devianest.u3app.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.U3App.NoActionBar" />

        <!-- BaseActivity -->
        <activity
            android:name="com.devianest.u3app.activity.BaseActivity"
            android:exported="false" />
    </application>

</manifest>